---
'@arkite-ui/core': patch
---

Republish of 0.6.0 (its publish pipeline failed before reaching the registries — no library changes). CI now pins pnpm via `packageManager` and runs on Node 22 images.
